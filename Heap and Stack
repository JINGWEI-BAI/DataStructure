1.栈stack是一种运算受限的线性表，意思是它仅允许在标的一段进行插入和删除操作，即栈顶Top. 
2.它的一般操作是，新进元素被压入Top的上面，使之成为新晋的栈顶元素。这一过程被称为，进栈push.
3.把栈顶元素删除，让与它相邻的元素成为新的栈顶元素，称为出栈pop.
4.push和pop的操作，这样的运算使得stack具备先进后出的特点First In Last Out.

1.堆heap是一种常见的树形结构，可以说，它是一种特殊的完全二叉树.
2.当且仅当满足下列条件，即所有的节点的值总是不大于或不小于其父节点得知的完全二叉树被称为堆.
3.heap的这个特性称之为堆序性.
4.在一个heap中，根节点是最大或最小节点。
5.如果根节点最小，称之为小顶堆。如果根节点最大，称之为大顶堆。
6.heap的左右孩子没有大小的顺序。


